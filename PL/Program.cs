using System;
using BlApi;
using BO;
using System.Collections.Generic;


namespace PL
{
    class Program
    {
        static IBl bl;

        static void Main(string[] args)
        {
            //bl = BlFactory.GetBl();
            //foreach (var b in bl.getScheduleForALine(2,877).DepartureLineTimes)
            //    Console.WriteLine(b);
            //foreach (var a in bl.GetAllLinesTimesInStation(877, new TimeSpan(23,57,0)))
            //{
            //   // Console.WriteLine(a.BusLineIdentificator);
            //    Console.WriteLine(a.TimingLeft);
            //}
            //Console.ReadKey();
        }
    }
}